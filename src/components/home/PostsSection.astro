---
import PostList from "~/components/blog/list/PostList.astro"
import LinkButton from "~/components/common/LinkButton"
import SectionHeader from "~/components/common/SectionHeader.astro"
import { featuredPosts } from "~/consts/featured"
import type { Post } from "~/content"

const filter = (post: Post) => featuredPosts.includes(post.id)
---

<section>
  <SectionHeader text="Featured posts" />
  <div class="flex justify-center py-4">
    <div>
      <PostList heading={3} {filter} />
    </div>
  </div>
  <div class="mb-4 flex justify-center">
    <LinkButton href="/blog">View all posts</LinkButton>
  </div>
</section>
